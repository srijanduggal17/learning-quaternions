
Say you have an orientation represented by $q$.

If you want to rotate $q$ about the global frame axes, you would left multiply: $q_{\Delta} \cdot q$

If you want to rotate $q$ about the local / body frame axes, you would right multiply: $q \cdot q_{\Delta}$

